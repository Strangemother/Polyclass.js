# parent selector

Assign a parent CSS selector to all built classes

    parentSelector: ".acme"
    Selector "margin-top-1em"

CSS:

    .acme .margin-top-1em {
        margin-top: 1em;
    }
