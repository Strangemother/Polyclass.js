import { DynamicCSSStyleSheet, RenderArray } from './dcss/dcss-main.js'; // Adjust the path as needed

// Export the modules or components that should be accessible to users
export {
  DynamicCSSStyleSheet, RenderArray
};


// Export the main functionality and any other components
module.exports = {
  greet
};